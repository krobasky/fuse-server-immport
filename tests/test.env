#warning: variable values must not contain spaces, per export in tests/test*.sh
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1
API_PORT=8082
# uncomment for outputting tests in tap-stream format
# TAP_STREAM=--tap-stream
